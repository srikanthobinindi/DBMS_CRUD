<%- include('partials/_header'); -%>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-12">
            <h2>Edit Pharmacist</h2>
            <a href="/pharmacists" class="btn btn-secondary mb-3">Back to Pharmacists</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <form action="/pharmacists/update/<%= data.pharmacist_id %>" method="post">
                        <div class="form-group">
                            <label for="pharmacist_id">Pharmacist ID</label>
                            <input type="number" name="pharmacist_id" value="<%= data.pharmacist_id %>" class="form-control" readonly>
                        </div>
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" name="name" value="<%= data.name %>" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="email" name="email" value="<%= data.email %>" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Phone</label>
                            <input type="text" name="phone" value="<%= data.phone %>" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="admin_id">Admin</label>
                            <select name="admin_id" class="form-control">
                                <option value="">-- Select Admin --</option>
                                <% if (admins) { %>
                                    <% for (var i = 0; i < admins.length; i++) { %>
                                        <option value="<%= admins[i].admin_id %>" <%= (data.admin_id == admins[i].admin_id) ? 'selected' : '' %>>
                                            <%= admins[i].name %>
                                        </option>
                                    <% } %>
                                <% } %>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            Update Pharmacist
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('partials/_footer'); -%>
